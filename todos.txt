

add a script to export test data


add a zooming in function.  For both test types, when the user hover the mouse on one of the image, show a n times zoomed in version of the image region around the mouse on a cornor of each image shown in the current page.  The region to show the zoomed-in imag should have 1/3 width and 1/3 height of the image, and it should located on the cornor that is farthest away from  the mouse cursor. 
This function needs to be enabled in experiment settings. 





add hotkeys: current settigs already works for the MOS mode if using textbox input.  When using button inputs AND the inputs range is within 1-9, bind each key to corresponding button.  Bind enter to submit.  For 2AFC, bind "A" and "1" to button and "B" and "2" to button 2. 


python scripts/generate_dev_test.py '/mnt/data/datasets/IQA_DBs/new_db_training/databaserelease2' 
python3 manage.py import_study scripts/live_dev_mos.json --activate

python scripts/generate_dev_test.py \
  --cross-ref 0.5 \
  --mode 2AFC \
  --outdir images/live_dev-2afc \
  '/mnt/data/datasets/IQA_DBs/new_db_training/databaserelease2' \
